# Ralph Run: Slug Utility Hardening

Objective:
Implement `src/slugify.py` that reads `input.txt` and writes deterministic slug output and `report.json`.

Success criteria:
- normalized slugs are deterministic (sorted + deduped)
- Unicode normalization is applied
- invalid lines are omitted and counted in report

Completion phrase: TASK_DONE
