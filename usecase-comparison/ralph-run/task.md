# Ralph Run: Slug Utility

Objective:
Implement `src/slugify.py` that reads `input.txt`, converts each line to kebab-case,
removes duplicates, sorts, and writes `output.txt`.

Criteria for completion:
- stable output file with deterministic ordering
- no duplicates
- script handles punctuation/extra spaces

Completion phrase: TASK_DONE
